# DecodeVision sample data

`samples/decodevision_invoice.png` is a deterministic synthetic invoice used to
demonstrate the OCR pipeline without downloading user data.

Regenerate it with:

```powershell
python scripts/generate_sample.py
```

The image contains no real customer, financial or personal information.
